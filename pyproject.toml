[project]
name = "osirisdata"
version = "1.0"
readme = "README.md"
requires-python = ">= 3.12"
dependencies = [
  "pymongo",
  "nameparser",
  "levenshtein",
  "requests",
  "pydantic",
  "pydantic-extra-types"
]
license = "MIT"