[project]
name = "osirisdata"
version = "0.0.0"
readme = "README.md"
requires-python = ">= 3.8"
dependencies = [
  "pymongo",
  "nameparser",
  "levenshtein",
  "requests",
  "pydantic",
  "pydantic-extra-types"
]
license = "MIT"